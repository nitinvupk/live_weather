<header>
  <nav class="navbar navbar-expand-md fixed-top navbar-dark">
    <div class="container">
      <%= link_to t('app.name'), root_path, id: "logo", class: "navbar-brand" %>
       <%= render 'shared/add_city_form' %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item"><%= link_to t('menu.home'), root_path, class: "nav-link #{active?(root_path)}" %></li>
          <li class="nav-item"><%= link_to t('menu.help'), help_path, class: "nav-link #{active?(help_path)}" %></li>
          <li class="nav-item"><%= link_to 'Sign Up', new_user_registration_path, class: "nav-link #{active?(new_user_registration_path)}" if !current_user %></li>
          <li class="nav-item"><%= link_to 'Login', new_user_session_path, class: "nav-link #{active?(new_user_session_path)}" if !current_user %></li>
          <li class="nav-item"><%= link_to "Logout", destroy_user_session_path, :method => :delete, class: "nav-link #{active?(destroy_user_session_path)}" if current_user %></li>
        </ul>
      </div>
    </div>
  </nav>
</header>
